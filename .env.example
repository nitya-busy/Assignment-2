# Banking System - Environment Configuration Template

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=banking_system
DB_SSLMODE=disable

# Server Configuration
PORT=8080

# Note: Update these values as per your PostgreSQL installation
# For production, use environment-specific values
